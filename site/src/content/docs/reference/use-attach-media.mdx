---
title: useAttachMedia
description: Hook that attaches an HTML media element to a playback adapter
---

import UtilReference from "@/components/docs/api-reference/UtilReference.astro";

## Import

```tsx
import { useAttachMedia } from "@videojs/react";
```

## Usage

Pass a playback adapter to receive a callback ref for its rendered `<video>` or `<audio>` element.

```tsx
import { HlsJsAdapter } from "@videojs/hlsjs-video";
import { useAttachMedia, useMediaInstance } from "@videojs/react";

function CustomHlsVideo() {
  const media = useMediaInstance(HlsJsAdapter);
  const attachRef = useAttachMedia(media);

  return <video ref={attachRef} />;
}
```

The callback attaches the element it receives to the playback adapter and detaches it when the element or the adapter changes, and on unmount. React handing the ref `null` and then the same element again, which it does when a composed ref changes identity, leaves the attachment alone.

<UtilReference util="useAttachMedia" />
